package com.aresudev.loompaap.core.utils

class TimeUtils {
    companion object{
        const val TIME_ZERO: Long = 0
        const val ONE_SECOND_IN_MILLIS: Long = 1000
        const val ONE_MINUTE_IN_MILLIS: Long = ONE_SECOND_IN_MILLIS * 60
        const val FIVE_MINUTES_IN_MILLIS: Long = ONE_MINUTE_IN_MILLIS * 5
    }
}